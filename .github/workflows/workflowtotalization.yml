name: workflow totalization

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  workflowtotalization:
    runs-on: ubuntu-latest
    steps:   
      - uses: actions/github-script@v6
        with:
          script: |
            const result = await github.rest.actions.listRepoWorkflows({
              owner: context.repo.owner,
              repo: context.repo.repo,
            })
            const list = result.data.workflows;
            console.log(JSON.stringify(list));
            const output = "XXX";
            for(let workflow of list){
              output += workflow.id + "\n";
              output += workflow.name + "\n";
            }
            await core.summary
              .addHeading('Workflows report')
              .addRaw(output)
              .write()
